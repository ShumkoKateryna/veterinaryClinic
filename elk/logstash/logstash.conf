input {
  tcp {
    port => 5000
    codec => json_lines
  }

  http {
    port => 8080
    host => "0.0.0.0"
  }
}

output {
  elasticsearch {
    hosts => ["https://onpu-search-4730339165.eu-central-1.bonsaisearch.net:443"]
    user => "wqq66uwm8s"
    password => "fdb9a8mad5"
    index => "java-app-logs"
    ssl => true
  }
}